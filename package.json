{
  "name": "speakflow",
  "version": "1.0.0",
  "description": "A lightweight web app that converts Chinese scenes or awkward English into natural, idiomatic English expressions",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
